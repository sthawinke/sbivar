% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{computeIxyAndTracePPP_cpp}
\alias{computeIxyAndTracePPP_cpp}
\title{Build the Gaussian weight matrix and compute Ixy + variance traces for MoransISinglePPP}
\usage{
computeIxyAndTracePPP_cpp(Cx, Ey, eta, Y, vgParY, sqrtProdFac, findVariances)
}
\arguments{
\item{Cx}{\eqn{n \times 2} X-point coordinates for the current cell type}

\item{Ey}{\eqn{m \times 2} Y-spot coordinates}

\item{eta}{Gaussian bandwidth \eqn{\eta}}

\item{Y}{\eqn{m \times k} scaled Y feature matrix (columns = \code{featuresY})}

\item{vgParY}{\eqn{k \times 3} variogram parameters \code{[psill, range, isExp]}
(ignored when \code{findVariances = FALSE})}

\item{sqrtProdFac}{\eqn{\sqrt{(n-1)(m-1)}} normalisation factor}

\item{findVariances}{logical; whether to compute variance traces}
}
\value{
A list with
  \describe{
    \item{isZero}{logical; \code{TRUE} if \eqn{W} sums to zero (all weights underflow)}
    \item{Ixys}{length-\eqn{k} vector of Ixy statistics (zero when \code{isZero})}
    \item{traces}{length-\eqn{k} vector of \eqn{tr(W^T \Sigma_X W)} (zero when
      \code{!findVariances} or \code{isZero})}
    \item{trWtW}{\eqn{tr(W^T W) = \sum W_{ij}^2}, used as the independence fallback}
  }
}
\description{
Combines weight-matrix construction, Ixy calculation, and variance-trace
computation in a single C++ call so that the \eqn{n \times m} weight matrix
\eqn{W} is never materialised as an R object.  The weight matrix is
\eqn{W_{ij} \propto \exp(-\|C_x^{(i)} - E_y^{(j)}\|^2 / \eta)}, normalised
to sum to one.
}
\keyword{internal}
